[ -n "$PS1" ] && source ~/.bash_profile

export NVM_DIR=~/.nvm
source $(brew --prefix nvm)/nvm.sh

eval "$(ssh-agent -s)"
ssh-add -K ~/.ssh/beauknowssoftware_rsa

if [ -f ~/.bashrc.local ]; then
    source ~/.bashrc.local
fi
